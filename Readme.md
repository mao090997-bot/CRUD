Usuario hace clic en un botón o envía un formulario
        ↓
JavaScript captura el evento (click, submit, input, etc.)
        ↓
Se envía una solicitud HTTP al servidor con fetch()
        ↓
El servidor responde con los datos actualizados
        ↓
JavaScript actualiza el DOM y muestra los cambios en la página

